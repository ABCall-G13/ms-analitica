[tox]
envlist = py39

[testenv]
setenv =
    PYTHONPATH = {toxinidir}
deps =
    -requirements.txt
commands =
    python3 -m pytest --cov=app --cov-report=xml tests/

[coverage:run]
relative_files = True
source = app/
branch = True
